(N, M) = map(int, input().split())

first_set = set()
second_set = set()

for _ in range(N):
    first_set.add(input())

for _ in range(M):
    second_set.add(input())

print('\n'.join(first_set & second_set))
